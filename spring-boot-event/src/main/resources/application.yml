
spring:
  application:
    name: spring-boot-event
  # 数据库链接
  # 记得修改成自己的！！！
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://127.0.0.1:3306/demo?autoReconnect=true&characterEncoding=utf8&useSSL=false&allowMultiQueries=true&rewriteBatchedStatements=true
    username: root
    password: root
    hikari:
      max-lifetime: 500000
  # redis链接
  # 记得修改成自己的！！！
  redis:
    host: 127.0.0.1
    port: 6379
    database: 4
server:
  port: 8088
